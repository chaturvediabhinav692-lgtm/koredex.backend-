{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn api:app --host 0.0.0.0 --port $PORT"
  },
  "nixpacks": {
    "providers": ["python"],
    "variables": {
      "PYTHON_VERSION": "3.11"
    }
  }
}